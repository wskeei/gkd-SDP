# Privacy Notes

Last updated: 2026-08-09

本文说明 GKD-SDP 当前代码和权限模型下的隐私边界，不是对所有 Android/OEM 行为的绝对保证。具体实现变化时必须同步更新本文。

## 本机可能处理的数据

为实现自动化和数字自律功能，应用可能在设备本地处理：

- 已安装应用及前台应用状态；
- 无障碍节点、快照和订阅规则匹配数据；
- 应用拦截、URL 拦截、专注会话和自动重开配置；
- 使用申请的理由、标签、时长、开始/结束记录和复盘数据；
- 类型化诊断事件、脱敏崩溃摘要和用户主动生成的支持包。

本版本的间隔洞察还会在本机保存最小的事实字段：触发记录的动作结果和订阅/规则组/规则名称快照，
以及使用申请的最后一次可确认结束时间和冻结的未使用间隔。间用比和滚动平均均在本机计算。
这些字段不用于上传，也不包含 selector 文本、无障碍节点文本、实际 URL、URL pattern、截图、设备 ID
或新的申请理由副本；申请理由仍只在既有 `UsageGuardRecord` 中作为申请事实和活动提醒来源。

这些数据用于本地规则判断、显示、恢复和用户主动导出。项目不应把申请理由、屏幕文本或 URL 作为普通遥测上传。应用保留 Android 系统备份能力，但使用显式白名单，只允许无敏感内容的主题、显示密度和语言偏好进入云备份或设备迁移；数据库、普通/私有 Store 的其余内容、订阅、日志、崩溃、截图、缓存、命令文件和密钥均被排除。

本地记录有界保留：ActionLog 继续按 100 行批次裁剪到最近 500 行（批次间最多 599 行）；拦截事件和 exact-rule
latest-state 分别保持 90 天、10,000 行上限。新增统计不会扩大既有保留期限或采集内容。

## 网络访问与本地 HTTP 服务

网络访问可能用于用户配置的订阅/规则更新、上游或项目文档链接、以及 GKD-SDP GitHub Releases 的版本检查和 APK 下载。应用内版本检查会访问公开的 GitHub API 和 Release asset；APK 不内置 GitHub token，也不会把用户的 Cookie/Authorization header 当作版本检查凭据。网络服务仍可能看到 IP、时间、User-Agent 等请求元数据；用户配置的订阅/CDN/文档服务还可能按其自身策略记录请求，这些第三方日志不由项目控制。

应用还可以由用户在高级设置中启用本地 Inspector。服务默认只监听 `127.0.0.1`；只有用户点击“开启 15 分钟局域网调试”后才临时监听局域网，并在锁屏、停止、到期、切回仅本机或立即断开时撤销会话。浏览器需在 60 秒内输入应用显示的 8 位一次性配对码；成功后签发的随机令牌只绑定首个客户端 IP、User-Agent 摘要和精确 Origin，绝对有效期不超过 15 分钟。同一时刻只保留一个会话。

Inspector 默认只允许读取服务信息和快照列表。查看/捕获/删除快照、更新内存订阅和执行选择器分别由本机开关授权，并受总请求、捕获、执行、请求体和文件响应限额约束。静态 Inspector 随 APK 提供，不加载远程脚本；API 错误不返回路径、Uri、异常或堆栈。启用敏感 scope 后，相应内容会离开应用进程并显示在已配对浏览器中，因此只在可信设备和网络中临时开启。

用户添加 `http://` 订阅时，应用展示规范化的 scheme、host、port 和明文风险，并只在明确确认后保存该单一来源授权；path、query 和凭据不写入授权键。每次重定向都会重新检查来源，撤销后后续请求立即拒绝。该授权不适用于 WebView 或更新检查；项目固定地址继续使用 HTTPS。HTTP 传输本身不加密，网络参与方仍可能读取或修改订阅内容。

应用内文档 WebView 只允许 `https://gkd.li` 主框架，固定 HTTPS 镜像响应也经过来源、MIME、大小和 CSP 检查。其他 HTTPS 导航交给外部浏览器；HTTP、file、content、data、javascript、intent 和未知 scheme 被拒绝。WebView 不安装 JavaScript bridge，并关闭文件/内容访问、混合内容和第三方 Cookie。

用户主动分享支持包、快照或导出文件时，数据也可能离开设备。应用生成的支持包或备份可能位于 cache/shared 目录，并可能被用户通过系统分享、文件管理器或其他备份机制保存到 app 外部；保存后的文件不会随着清除 app-private 数据自动删除。Android/OEM 的系统备份只应读取下述显示偏好白名单，具体传输与保留仍由用户配置的系统备份目的地控制。

## 支持包白名单

支持包只在用户查看内容与隐私说明、再次确认具体分享或保存操作后生成。压缩包固定只包含：

- manifest.json：格式版本、应用版本、flavor、Android API、分钟级生成时间和其他条目的 SHA-256；
- app-summary.json：安装来源类别、应用版本、主 ABI、Android API 和功能开关布尔值；
- capability-summary.json：权限与可选系统能力的布尔状态；
- diagnostic-events.jsonl：最多 500 条类型化、分钟级、脱敏诊断事件；
- crash-summary.json：错误码、错误类别、分钟级时间、应用内方法名和计数。

支持包不会递归复制数据库、Store、订阅、原始日志或原始 crash 文件，也不会包含申请理由、完整 URL 或 pattern、截图、无障碍节点文字、通知正文、联系人、Cookie、Authorization、设备标识或文件绝对路径。支持包在 app 外部保存或上传后的副本由接收位置控制，应用无法替用户删除。

## 手工加密备份

设置页生成的手工备份使用 `GKDSDPBK2` 格式：用户选择数据类别并输入至少 12 个 Unicode 字符的密码后，应用使用 PBKDF2-HMAC-SHA256（600,000 次）派生 256-bit 密钥，并使用 AES-256-GCM 加密完整 payload。密码只用于当次导出或导入，不写入日志、Store、备份 header 或其他持久化位置；应用无法找回遗失的密码。

默认备份包含普通设置、订阅、自律配置、自律历史和上游历史。快照及其文件、无障碍事件日志和微信联系人属于单独的“敏感可选数据”，默认关闭并在导出前显示说明。诊断日志、崩溃文件、缓存、支持包、私有 Store、安装级诊断盐、远程会话、密钥和命令脚本始终排除。明文 header 只包含格式、密码派生参数、随机 salt/nonce 和密文长度；类别清单、对象数量、内容和对象摘要均在认证加密范围内。

导入先完成解密、格式/schema、对象摘要、引用关系和冲突预览，不在预览阶段修改数据；二次确认后只替换备份包含的类别。导入过程写入私有恢复日志，任一步失败或进程在应用阶段退出时恢复导入前的受影响 Room 行与普通 Store。旧版未加密 ZIP 只读取固定旧路径，先转换为相同 v2 catalog，再经过同一校验、预览和回滚流程；旧文件本身仍是明文，导入后应重新导出为 v2 并安全删除旧副本。

备份保存到用户在系统文件选择器中指定的位置。离开 app-private 目录后的备份由目标存储、同步服务和用户设备控制；即使内容已加密，文件名、大小和保存时间等元数据仍可能对该位置可见，清除应用数据也不会删除这些外部副本。

## Android 系统备份与设备迁移

Android 云备份和设备迁移使用同一白名单，仅包含 `app_theme.json`、`display_density.json` 和 `language.json` 三项显示偏好。恢复这些文件只改变主题、显示密度或语言，不会自动开启无障碍、Automation/Shizuku、悬浮窗、通知、锁定、拦截、HTTP 服务或其他运行能力。

系统备份不等同于上方的手工加密备份 v2：系统路径由 Android/OEM 和用户的备份账户控制，应用不提供独立密码或完整配置恢复；手工 v2 由用户选择类别和保存位置，内容在离开应用前完成认证加密。需要迁移订阅、自律配置或历史时使用手工 v2，不应依赖系统备份。

## 系统权限（非穷尽清单）

- Accessibility：读取系统提供的界面事件/节点并执行用户配置的规则。
- Draw over other apps：显示拦截、申请和安全提示覆盖层。
- Notifications/foreground service：显示提醒和维持需要的后台工作。
- Shizuku（可选）：执行用户明确启用的增强系统动作。
- Internet：更新订阅、访问配置的网络资源和检查 Release。
- Query installed packages / installed apps：枚举已安装应用以提供规则选择、应用拦截和前台应用判断；这是高敏感的应用列表数据。
- Usage/app-ops related access：在用户启用相应能力并由系统允许时读取使用统计或 AppOps 状态。
- Secure settings / battery optimization / media projection：只在用户明确授权、Shizuku 或系统流程允许时用于高级设置、后台稳定性和截图/调试能力。

未授予相关权限时，功能应显示受限状态或安全失败；权限本身不会被代码静默升级。

## 截图与安全窗口

当前倒计时悬浮条使用 `FLAG_SECURE` 保护窗口中的倒计时和理由。申请输入页、超时拦截页和其他 Overlay 是否受截图保护，以各自 WindowManager flags 和设备/OEM 行为为准，不能默认当作安全窗口；如果某个页面需要完全隐身，必须先在代码中加上 `FLAG_SECURE` 并完成真机验证。Android/OEM 可能显示黑块、空白或拒绝截屏；项目不承诺能够控制第三方应用内容或所有系统录屏行为。

## 数据控制

“设置 → 隐私与数据”页按以下类别显示本地保留范围：使用申请历史、专注会话历史、拦截与触发记录、应用安装监控历史、快照与截图、事件/活动日志、诊断与崩溃摘要、订阅与规则配置、数字自律配置、全部应用数据。页面只显示记录数量、磁盘占用和脱敏摘要，不显示申请理由、URL、规则 pattern、无障碍节点文本、联系人或其他敏感 payload。

历史删除只清除对应记录，不改变 `enabled`、`locked`、每日关闭额度、自动重开和规则配置；快照删除会同步数据库行与本地文件。配置与全部数据删除需要显式确认，并在存在活动使用申请、专注会话或锁定保护时阻断。

用户也可以在任何页面通过系统设置撤销权限、在应用内删除/清理可用记录，或通过 Android 应用设置清除 app-private 数据。清除 app-private 数据通常会删除本地配置、数据库、日志和申请记录，但不会替用户删除已经导出到共享存储、云备份或其他 app 的副本；具体页面以当前版本 UI 为准。

应用 cache 中的临时共享文件会按应用自己的清理策略过期；数据库、配置、日志和申请记录通常会保留到用户主动清理、清除 app data 或卸载应用。用户导出的备份/日志、Android 系统备份中的三项显示偏好以及 GitHub/第三方服务端的副本分别遵循各自的删除和保留策略，项目无法替用户删除这些外部副本。

## 未来变化

当前计划不加入自有广告、行为分析、Sentry、Firebase Analytics 或其他商业遥测 SDK。任何未来遥测都必须先公开设计、默认关闭、最小化数据并更新本文。
