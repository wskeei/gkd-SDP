# Privacy Notes

本文说明 GKD-SDP 当前代码和权限模型下的隐私边界，不是对所有 Android/OEM 行为的绝对保证。具体实现变化时必须同步更新本文。

## 本机可能处理的数据

为实现自动化和数字自律功能，应用可能在设备本地处理：

- 已安装应用及前台应用状态；
- 无障碍节点、快照和订阅规则匹配数据；
- 应用拦截、URL 拦截、专注会话和自动重开配置；
- 使用申请的理由、标签、时长、开始/结束记录和复盘数据；
- 应用运行日志、崩溃信息和调试导出文件。

这些数据用于本地规则判断、显示、恢复和用户主动导出。项目不应把申请理由、屏幕文本或 URL 作为普通遥测上传。应用的 Android manifest 当前允许系统备份（`android:allowBackup="true"`），具体 Android/OEM 备份策略可能将 app-private 数据带到用户配置的备份目的地；这不等同于项目主动上传，仍应视为数据离开设备的路径。

## 网络访问与本地 HTTP 服务

网络访问可能用于用户配置的订阅/规则更新、上游或项目文档链接、以及 GKD-SDP GitHub Releases 的版本检查和 APK 下载。应用内版本检查会访问公开的 GitHub API 和 Release asset；它不携带 GitHub token。

应用还可以由用户在高级设置中启用本地 HTTP 服务。启用后，同一局域网内能访问该端口的客户端可能读取或提交应用提供的订阅、快照、选择器/调试数据；这不是 GitHub 云端上传，但仍是数据离开本机进程边界的路径。只在可信网络启用，并在不需要时关闭；不要把该端口暴露到不可信网络。

用户主动分享日志、快照或导出文件时，数据也可能离开设备。应用生成的备份/日志压缩包可能位于 cache/shared 目录，并可能被用户通过系统分享、文件管理器或其他备份机制保存到 app 外部；保存后的文件不会随着清除 app-private 数据自动删除。Android/OEM 的系统备份也可能按用户配置复制 app-private 数据。分享前请自行删除申请理由、账号、URL 凭据/私密参数、屏幕文本和其他个人数据。

## 系统权限（非穷尽清单）

- Accessibility：读取系统提供的界面事件/节点并执行用户配置的规则。
- Draw over other apps：显示拦截、申请和安全提示覆盖层。
- Notifications/foreground service：显示提醒和维持需要的后台工作。
- Shizuku（可选）：执行用户明确启用的增强系统动作。
- Internet：更新订阅、访问配置的网络资源和检查 Release。
- Query installed packages / installed apps：枚举已安装应用以提供规则选择、应用拦截和前台应用判断；这是高敏感的应用列表数据。
- Usage/app-ops related access：在用户启用相应能力并由系统允许时读取使用统计或 AppOps 状态。
- Secure settings / battery optimization / media projection：只在用户明确授权、Shizuku 或系统流程允许时用于高级设置、后台稳定性和截图/调试能力。

未授予相关权限时，功能应显示受限状态或安全失败；权限本身不会被代码静默升级。

## 截图与安全窗口

当前倒计时悬浮条使用 `FLAG_SECURE` 保护窗口中的倒计时和理由。申请输入页、超时拦截页和其他 Overlay 是否受截图保护，以各自 WindowManager flags 和设备/OEM 行为为准，不能默认当作安全窗口；如果某个页面需要完全隐身，必须先在代码中加上 `FLAG_SECURE` 并完成真机验证。Android/OEM 可能显示黑块、空白或拒绝截屏；项目不承诺能够控制第三方应用内容或所有系统录屏行为。

## 数据控制

用户可以在系统设置撤销权限、在应用内删除/清理可用记录，或通过 Android 应用设置清除 app-private 数据。清除 app-private 数据通常会删除本地配置、数据库、日志和申请记录，但不会替用户删除已经导出到共享存储、云备份或其他 app 的副本；具体页面以当前版本 UI 为准。

## 未来变化

当前计划不加入自有广告、行为分析、Sentry、Firebase Analytics 或其他商业遥测 SDK。任何未来遥测都必须先公开设计、默认关闭、最小化数据并更新本文。
