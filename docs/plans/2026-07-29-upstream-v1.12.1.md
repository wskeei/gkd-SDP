# GKD v1.12.1 Upstream Integration Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Integrate upstream GKD `v1.12.1` into GKD-SDP without regressing any SDP feature or data compatibility.

**Architecture:** Preserve the real upstream merge ancestry and resolve the namespace-aware three-way merge module by module. Keep SDP product identity and restriction engines as invariants while adopting upstream core, Android compatibility, Shizuku, subscription, snapshot, and UI fixes.

**Tech Stack:** Android, Kotlin, Jetpack Compose, Room, KSP, Gradle, Kotlin Multiplatform selector module, GitHub Actions.

---

### Task 1: Record the integration baseline

**Files:**
- Create: `docs/plans/2026-07-29-upstream-v1.12.1-design.md`
- Create: `docs/plans/2026-07-29-upstream-v1.12.1.md`

**Step 1: Verify the target**

Run: `git rev-parse upstream-v1.12.1`
Expected: `5a00f844...`

**Step 2: Verify the isolated branch**

Run: `git status --short --branch`
Expected: branch `merge/upstream-v1.12.1` with only the two plan files.

**Step 3: Commit**

```bash
git add docs/plans/2026-07-29-upstream-v1.12.1-design.md docs/plans/2026-07-29-upstream-v1.12.1.md
git commit -m "docs: plan upstream v1.12.1 integration"
```

### Task 2: Start the real upstream merge

**Files:**
- Modify: all paths changed by the three-way merge from `dcae9197` to `upstream-v1.12.1`

**Step 1: Merge without committing**

Run: `git merge --no-commit upstream-v1.12.1`
Expected: merge stops with the conflicts identified by `git merge-tree`.

**Step 2: Inventory conflicts**

Run: `git diff --name-only --diff-filter=U`
Expected: conflicts grouped under workflows, Gradle, app core, a11y, Shizuku, UI, and utilities.

**Step 3: Preserve the merge state**

Do not commit until every unmerged index entry is resolved.

### Task 3: Resolve build identity and dependency infrastructure

**Files:**
- Modify: `.github/workflows/Build-Apk.yml`
- Modify: `.github/workflows/Build-Release.yml`
- Modify: `build.gradle.kts`
- Modify: `app/build.gradle.kts`
- Modify: `gradle/libs.versions.toml`
- Modify: `gradle/wrapper/gradle-wrapper.properties`
- Modify: `app/src/main/AndroidManifest.xml`
- Modify: `app/src/gkd/AndroidManifest.xml`
- Modify: `hidden_api/build.gradle.kts`
- Modify: `selector/build.gradle.kts`

**Step 1: Lock product invariants**

Keep `applicationId = "li.songe.gkd.sdp"`, SDP namespace, both product flavors, SDP release workflows, and optional signing behavior.

**Step 2: Adopt upstream build requirements**

Port upstream compile/target SDK, Gradle, AGP, Kotlin, Compose, navigation, hidden API, and dependency changes only where compatible with newer versions already present in SDP.

**Step 3: Check configuration**

Run: `rg -n '^(<<<<<<<|=======|>>>>>>>)' .github app/build.gradle.kts build.gradle.kts gradle hidden_api selector`
Expected: no output.

**Step 4: Stage resolved infrastructure**

Run: `git add .github build.gradle.kts app/build.gradle.kts gradle app/src/main/AndroidManifest.xml app/src/gkd/AndroidManifest.xml hidden_api selector`

### Task 4: Resolve hidden API and Shizuku integration

**Files:**
- Modify: `hidden_api/src/main/java/**`
- Modify: `app/src/main/aidl/li/songe/gkd/sdp/shizuku/**`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/shizuku/**`

**Step 1: Adopt upstream Android compatibility APIs**

Port the upstream automation, UI automation connection, input, package/user manager, screenshot, and Android 17 compatibility implementations into the SDP namespace.

**Step 2: Preserve SDP callers**

Ensure Focus Mode, app detection, blockers, accessibility state correction, and existing Shizuku-backed helpers still compile against the migrated APIs.

**Step 3: Check package consistency**

Run: `rg -n 'li\\.songe\\.gkd(?!\\.sdp)' app/src/main/aidl/li/songe/gkd/sdp app/src/main/kotlin/li/songe/gkd/sdp/shizuku --pcre2`
Expected: no accidental old application-package references.

**Step 4: Stage and commit checkpoint after the merge becomes committable**

The final merge commit will contain all conflict resolutions; follow it with focused correction commits if compilation or tests expose issues.

### Task 5: Resolve app bootstrap, accessibility, and service runtime

**Files:**
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/App.kt`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/MainActivity.kt`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/MainViewModel.kt`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/a11y/**`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/service/**`
- Test: `app/src/test/kotlin/li/songe/gkd/sdp/**`

**Step 1: Use upstream runtime fixes as the base**

Integrate multi-display filtering, updated accessibility state detection, suspend actions, automation mode, track service, package-added handling, and screenshot compatibility.

**Step 2: Reconnect SDP engines**

Verify `FocusModeEngine`, `AppBlockerEngine`, `UrlBlockerEngine`, usage guard countdown/overlay, auto re-enable, and install monitor still initialize and receive the expected activity/accessibility signals.

**Step 3: Add or update regression contracts**

Tests must assert that bootstrap and accessibility wiring retain all SDP engine hooks and teardown behavior.

**Step 4: Scan for merge damage**

Run: `rg -n '^(<<<<<<<|=======|>>>>>>>)' app/src/main/kotlin/li/songe/gkd/sdp/{App.kt,MainActivity.kt,MainViewModel.kt,a11y,service}`
Expected: no output.

### Task 6: Resolve data, storage, subscription, snapshot, and backup behavior

**Files:**
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/data/**`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/store/**`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/util/BackupUtils.kt`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/util/SnapshotExt.kt`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/util/SubsState.kt`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/db/**` only if integration requires it
- Test: `app/src/test/kotlin/li/songe/gkd/sdp/**`

**Step 1: Port upstream model changes**

Integrate subscription cleanup, disabled-rule filtering, selector action changes, snapshot changes, backup support, and storage fixes.

**Step 2: Preserve data compatibility**

Retain all SDP entities, migrations, DAO bindings, settings keys, private store locations, and external database path behavior.

**Step 3: Resolve upstream deletions deliberately**

Delete `TransferData.kt` only after all SDP references have migrated to the upstream backup model; otherwise retain an adapter.

**Step 4: Verify schemas and references**

Run: `rg -n 'TransferData|focus_session|app_group|url_block_rule|usageGuard' app/src/main app/src/test`
Expected: every retained feature has valid model and runtime references.

### Task 7: Resolve Compose UI and navigation

**Files:**
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/ui/**`
- Modify: `app/src/main/kotlin/li/songe/gkd/sdp/MainActivity.kt`
- Modify: `app/src/main/res/**`

**Step 1: Port upstream UI features**

Integrate accessibility scope UI, crash reports, category-group UI, tri-state controls, image preview updates, and upstream rule/config fixes.

**Step 2: Preserve SDP navigation**

Keep Focus, App Blocker, URL Blocker, Usage Guard, review dashboard, widget configuration, locks, and settings routes visible and reachable.

**Step 3: Preserve SDP resources**

Resolve strings, icons, XML services, file provider paths, overlays, widgets, and flavor manifests without deleting SDP resources.

**Step 4: Check duplicate and stale sources**

Run: `find app/src/main/kotlin/li/songe/gkd -type f | sort`
Expected: only files under `li/songe/gkd/sdp`; no parallel legacy source tree.

### Task 8: Complete and checkpoint the merge

**Files:**
- Modify: every remaining unmerged file

**Step 1: Confirm index resolution**

Run: `git diff --name-only --diff-filter=U`
Expected: no output.

**Step 2: Run repository-wide static checks**

```bash
rg -n '^(<<<<<<<|=======|>>>>>>>)' .
find app/src/main/kotlin/li/songe/gkd -type f | rg -v '/sdp/'
git diff --check
```

Expected: no conflict markers, no legacy Kotlin sources, no whitespace errors.

**Step 3: Commit the merge**

```bash
git add -A
git commit -m "merge: integrate upstream GKD v1.12.1"
```

### Task 9: Test custom feature invariants

**Files:**
- Test: `app/src/test/kotlin/li/songe/gkd/sdp/**`

**Step 1: Run policy test inventory**

Run: `find app/src/test -type f | sort`
Expected: tests cover focus, app blocker, URL blocker, usage guard, auto re-enable, action log, and selector integration contracts.

**Step 2: Run tests when Java is available**

```bash
bash gradlew :selector:jvmTest
bash gradlew :app:testGkdDebugUnitTest
```

Expected: both tasks pass. If local Java remains unavailable, do not claim test success; use GitHub Actions.

**Step 3: Commit test corrections**

```bash
git add app/src/test app/src/main
git commit -m "test: preserve SDP behavior after upstream merge"
```

### Task 10: Verify GitHub Actions build and finalize

**Files:**
- Modify only files required by verified CI failures.

**Step 1: Push the integration branch when authorized**

Run: `git push -u origin merge/upstream-v1.12.1`
Expected: branch is available to GitHub Actions.

**Step 2: Verify Actions**

Expected checks: GKD debug/release build, Play build where configured, app unit tests, and selector tests all succeed.

**Step 3: Fix failures in focused commits**

For each failure, use `superpowers:systematic-debugging`, reproduce or trace the root cause, patch minimally, and commit with a scoped message.

**Step 4: Final audit**

Run: `git log --oneline --decorate main..HEAD`
Expected: plan, upstream merge, and focused regression commits are reviewable separately.
