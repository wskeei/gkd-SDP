package li.songe.gkd.sdp.ui.style

import androidx.compose.ui.unit.dp

/** Fixed spacing, sizing and layout dimensions. */
object DimensionTokens {
    val SpacingXs = 4.dp
    val SpacingSm = 8.dp
    val SpacingMd = 12.dp
    val SpacingBase = 16.dp
    val SpacingLg = 20.dp
    val SpacingXl = 24.dp
    val SpacingXxl = 32.dp
    val SpacingXxxl = 40.dp
    val SpacingHuge = 48.dp

    /** Compact window horizontal page padding. */
    val PagePaddingCompact = 16.dp

    /** Medium window horizontal page padding. */
    val PagePaddingMedium = 24.dp

    /** Expanded window horizontal page padding. */
    val PagePaddingExpanded = 32.dp

    /** Max content width for forms. */
    val FormMaxWidth = 720.dp

    /** Max content width for lists and the review dashboard. */
    val ListMaxWidth = 960.dp

    /** Minimum touch target per the 48dp accessibility guidance. */
    val MinTouchTarget = 48.dp

    /** Small icon visual size. */
    val IconSizeSmall = 20.dp

    /** Default icon visual size. */
    val IconSizeDefault = 24.dp

    /** Top app bar height. */
    val TopBarHeight = 64.dp
}
